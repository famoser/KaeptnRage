<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:viewModel="using:Famoser.KaeptnRage.ViewModels"
            xmlns:viewModels="clr-namespace:Famoser.KaeptnRage.ViewModels;assembly=Famoser.KaeptnRage"
            x:Class="Famoser.KaeptnRage.View.Pages.MainPage">

  
  <ScrollView>
    <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />
    <ListView ItemsSource="{Binding PageItems}" x:Name="pageListView">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding Name}"
                    Detail="{Binding Author}"  />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </ScrollView>
</ContentPage>